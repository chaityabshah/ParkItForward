@{
    ViewBag.Title = "Delete event";
}
@model Planner.Models.Database.Event

<header>
    <h2>@ViewBag.Title</h2>
</header>

<br/>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        @using (Html.BeginForm("Delete", "Events", FormMethod.Post, new {@class = "form-horizontal"}))
        {
            @Html.Partial("_Details", Model)

            <input type="hidden" name="id" value="@Model.Id"/>
            <input type="hidden" name="name" value="@Model.Name"/>
            <button type="submit" class="btn btn-danger btn-block btn-sm">Confirm delete</button>
        }
    </div>
</div>